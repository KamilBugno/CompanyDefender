@using CompanyDefender.Models;
@using CompanyDefender.Converters;
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model PersonMailGraphViewModel

<!doctype html>
<html>
<head>
    <title>Mails viewer</title>

    <script type="text/javascript" src="~/Scripts/vis.js"></script>
   
    <link href="~/Content/my-css.css" rel="stylesheet" type="text/css" >

    <style type="text/css">
        #mynetwork {
            width: 1100px;
            height: 400px;
            border: 1px solid lightgray;
        }
    </style>
</head>
<body>
    <h2>System mailowy</h2>
    <div id="mynetwork"></div>
    <div class="row">
        <div class="column">
            <h2>Column 1</h2>
            <p>Some text 1..</p>
        </div>
        <div class="column">
            <h2>Column 2</h2>
            <p>Some text 2..</p>
        </div>
    </div>

    <script type="text/javascript">

        var persons = @JavaScriptConvert.SerializeObjectToJson(Model.PersonsMails);
        var nodes = new vis.DataSet(persons);

        var mails = @JavaScriptConvert.SerializeObjectToJson(Model.Mails);
        var edges = new vis.DataSet(mails);

        var container = document.getElementById('mynetwork');
        var data = {
            nodes: nodes,
            edges: edges
        };
        var options = {};
        var network = new vis.Network(container, data, options);
    </script>

</body>
</html>
